name: quick_remote_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^7.2.0
  provider: ^6.1.5+1
  sensors_plus: ^7.0.0
  vibration: ^3.1.8
  wakelock_plus: ^1.4.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
